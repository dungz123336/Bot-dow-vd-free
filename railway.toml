# Railway config — KHÔNG dùng healthcheck HTTP
# (Worker không có HTTP; Web healthcheck hay fail nếu app chậm boot)

[build]
builder = "NIXPACKS"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# Không set healthcheckPath / healthcheckTimeout
